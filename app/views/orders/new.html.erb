<% content_for :title, "New order" %>

<section class="page-header">
  <div>
    <h1>New order</h1>
    <p class="help-text">Create a new order for a customer.</p>
  </div>
  <div class="page-actions">
    <%= link_to "Back to orders", orders_path, class: "btn btn-ghost" %>
  </div>
</section>

<div class="card">
  <%= render "form", order: @order, show_status: true, submit_label: "Create order" %>
</div>
